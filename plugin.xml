<?xml version="1.0"?>
<zend-config xmlns:zf="http://framework.zend.com/xml/zend-config-xml/1.0/">
		<plugin>

            <!-- the plugin name [A-Za-z_] -->
            <pluginName>AssetThumbnailLink</pluginName>

            <!-- a short description -->
			<pluginDescription><![CDATA[A Pimcore plugin that provides a button to get absolute Thumbnail URLs]]></pluginDescription>

            <!-- meta data -->
            <pluginVersion>1.0</pluginVersion>
            <pluginRevision>1</pluginRevision>
            <pluginBuildTimestamp>0</pluginBuildTimestamp>

            <!-- put in the URL for the plugin configuration eg. /plugin/AssetThumbnailLink/admin/index -->
            <pluginIframeSrc></pluginIframeSrc>

            <pluginXmlEditorFile>/website/var/config/extension-assetthumbnaillink.xml</pluginXmlEditorFile>

            <!-- className of the plugin which extends Pimcore_API_Plugin_Abstract-->
			<pluginClassName>\AssetThumbnailLink\Plugin</pluginClassName>

            <!-- include paths relative to plugin-directory -->
			<pluginIncludePaths>
				<path>/AssetThumbnailLink/lib</path>
			</pluginIncludePaths>

			<!-- namespaces to register with autoloader-->
			<pluginNamespaces>
				<namespace>AssetThumbnailLink</namespace>
			</pluginNamespaces>

			<!-- js files with path relative to plugin-directory -->
			<pluginJsPaths>
                <path>/plugins/AssetThumbnailLink/static/js/startup.js</path>
			</pluginJsPaths>

			<!-- css files with path relative to plugin-directory -->
			<pluginCssPaths>
                <path>/plugins/AssetThumbnailLink/static/css/plugin.css</path>
			</pluginCssPaths>
		</plugin>
</zend-config>
